A simple library to learn Ajax
==============================

By going through the files here, one can learn basic steps
of making an ajax call for different kind of resources (text, html, json, xml etc.)
